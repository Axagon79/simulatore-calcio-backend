rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Accesso chiuso — riaprire collection specifiche quando necessario
    match /{document=**} {
      allow read, write: if false;
    }
  }
}