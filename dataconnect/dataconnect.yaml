specVersion: "v1beta"
serviceId: "pup-pals-backend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pup-pals-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
